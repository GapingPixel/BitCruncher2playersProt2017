/// @description Destroy surfaces, emitters & save
/*if (surface_exists(debris_surf)) {
   surface_free(debris_surf);
}

if (surface_exists(shader_surf)) {
   surface_free(shader_surf);
}

part_system_destroy(global.part_sys);

scr_save_game();

/* */
/*  */
